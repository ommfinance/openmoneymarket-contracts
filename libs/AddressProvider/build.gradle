version = '0.0.1'

dependencies {
    implementation project(':OMMStruct')
    implementation project(':ScoreUtils')
}

tasks.named('test') {
    // Use JUnit Platform for unit tests.
    useJUnitPlatform()
}
